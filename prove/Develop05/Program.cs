using System;

/*
    Exceeding Expectations --

    I exceeded expecatations by not allowing the user to copmlete an already completed goal, and added an option in the menu to delete a goal.

*/

class Program
{
    static void Main(string[] args)
    {
        Menu menu = new Menu();
        menu.Run();
    }
}